#version 330 compatibility

/* Legacy translucent route. Body shared with clrwl_gbuffers_translucent
   — the two programs MUST be pixel-identical. See
   lib/translucent_geometry_fsh.glsl. */

#include "/lib/settings.glsl"
#include "/lib/translucent_geometry_fsh.glsl"
