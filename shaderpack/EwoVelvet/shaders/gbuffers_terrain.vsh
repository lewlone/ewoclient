#version 330 compatibility

/* Legacy opaque terrain route. Body shared with clrwl_gbuffers — see
   lib/terrain_geometry_vsh.glsl. */

#include "/lib/settings.glsl"
#include "/lib/terrain_geometry_vsh.glsl"
