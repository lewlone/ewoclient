<!-- Superseded 2026-08-25: the continuation prompt lives at HANDOFF.md in the
     repo root and is rewritten every session. This file used to carry an
     M157-era snapshot; it is now a pointer so a stale copy cannot mislead. -->

The next-session prompt is **`HANDOFF.md`** at the repo root. Read it, then
`REWO_PLAN.md` §0.0 (technical entry point — current measurements, gate list,
live-check recipe, load-bearing gotchas) and `AGENT_LOOP_BRIEF.md` (process
rules). Do not restate numbers that live in §0.0.
