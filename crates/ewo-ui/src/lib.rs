//! `ewo-ui` — taffy layout, widgets, and screens.
//!
//! Composes `ewo-render` primitives into the UI components from the prototype:
//! glass-panel, vbtn, vslider, vdrop, vstatus, pbar (+ error variants), toggle,
//! pathfield, breathing-text, modal — and the four screens (main menu,
//! instances, settings, launching).
//!
//! See `CLAUDE.md` for the visual non-negotiables (motion is load-bearing,
//! never transform text-bearing containers, etc.).

// Modules will be filled in as the build sequence progresses (step 7 onwards):
//   pub mod ctx;
//   pub mod input;
//   pub mod widgets;
//   pub mod screens;
